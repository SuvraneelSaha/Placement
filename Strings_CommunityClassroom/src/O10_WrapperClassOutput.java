public class O10_WrapperClassOutput {
    public static void main(String[] args) {
        Integer num = new Integer(55);
        System.out.println(num.toString());
        // in case of python nothing is primitive every object is stored in heap memory


         // basically when we convert it into a wrapper class
        // we can use so many functions
        // we can use many functions with Integer wrapper class but not with int datatype
        // cause it is not an object
    }
}
