import java.lang.reflect.Array;
import java.util.ArrayList;

public class O17_Arraylists_Example {
    public static void main(String[] args) {

        ArrayList<Integer> list = new ArrayList<Integer>();
        // we can define the Datatype in the right hand side or maybe not
        // its not necessary
        // ie
        // Defining the datatype in the right hand side is not necessary

        /*
        Important - how to identify a Class - It Starts with a Capital letter

        ArrayList is a class which is present inside the collections framework


        Collection framework is just like the STL library in the C++

       it has all the predefined datastructures predefined already

       but in case of interviews we need to implement data structures from scratch only
         */
    }
}
